package com.kyilmaz.neuronetworkingtitle.ui.theme

import androidx.compose.ui.graphics.Color

// --- Neuro-Inclusive Primary Palette ---
// Soft, low-wavelength colors to reduce eye strain
val PrimaryViolet = Color(0xFF6750A4)
val PrimaryTeal = Color(0xFF3D7D75)
val PrimaryRose = Color(0xFF9C4146)

// --- Backgrounds ---
// Never pure white (#FFFFFF) or pure black (#000000) to reduce halation
val OffWhite = Color(0xFFFAFAFA)
val SoftPaper = Color(0xFFF5F2EF) // Warm tint for reading
val DeepCharcoal = Color(0xFF1C1B1F)
val SoftDark = Color(0xFF25232A)

// --- Functional Colors ---
val BubblyBlue = Color(0xFFD1E4FF)
val BubblyPink = Color(0xFFFFD8E4)
val BubblyYellow = Color(0xFFFFF9C4) // Good for highlighting without alarm

// --- Legacy Support (Kept for compatibility where needed) ---
val SoftRed = Color(0xFFFFB4AB)
val SoftOrange = Color(0xFFFFDBCC)
val Mint = Color(0xFFBCEBE1)
val Periwinkle = Color(0xFFC6D8FF)

// --- PostCard Specifics ---
val QUIET_NEUTRAL_LIGHT_BG = Color(0xFFF0F0F0)
val QUIET_NEUTRAL_DARK_BG = Color(0xFF121212)
val BUBBLE_YELLOW = BubblyYellow