package com.kyilmaz.neurocomet

// This file previously contained a duplicate `Post` model and a duplicate `NeuroCometLogo` composable.
// The canonical models and UI live in `DataModels.kt` and `MainActivity.kt`.
// Keeping this file empty avoids "Redeclaration" compiler errors while preserving path stability.
